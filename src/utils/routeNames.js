/**
 * list of all the routes in the application 
 * this is mapped in the route.js file with components
 * and used in all component when routing to any part of the file to avoid 
 * using strings 
 */
export const routeNames = {
    vote : "/",
}

